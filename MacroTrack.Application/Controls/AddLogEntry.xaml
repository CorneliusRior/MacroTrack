<UserControl x:Class="MacroTrack.AppLibrary.Controls.AddLogEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MacroTrack.AppLibrary.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{DynamicResource PanelBackGroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0"
                   Content="Add Log Entry"
                   Style="{StaticResource Label.Title}"/>

            <StackPanel Grid.Row="1"
                        Margin="10,10,10,10">
                <TextBlock Text="{Binding LastAdded}"
                           Margin="5"/>
                <TextBox x:Name="tbLog"
                         HorizontalAlignment="Stretch"
                         Height="100"
                         TextWrapping="Wrap"/>
                <Button x:Name="buttonAdd"
                        Content="Add"
                        HorizontalAlignment="Right"
                        Click="buttonAdd_Click"/>
            </StackPanel>

        

        </Grid>
    </Border>
</UserControl>
